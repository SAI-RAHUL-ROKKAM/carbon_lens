# Python 3.12 GPU-based requirements for the AIGA book examples/labs
# Install PyTorch GPU build first using the official selector, for example CUDA 12.1:
#   python -m pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#
# Then install the rest:
#   python -m pip install -r requirements-gpu.txt
#
# This file assumes an NVIDIA CUDA setup for QLoRA/bitsandbytes workflows.

# Core scientific Python
numpy>=2.0
scipy>=1.13
pandas>=2.2
matplotlib>=3.8
scikit-learn>=1.5

# Deep learning frameworks
# torch is intentionally commented out because CUDA wheels should be installed with the correct index URL.
# torch>=2.3
# torchvision>=0.18
# torchaudio>=2.3
tensorflow>=2.16

# NLP / embeddings / transformers
gensim>=4.3
transformers>=4.44
sentence-transformers>=3.0
tokenizers>=0.19
accelerate>=0.33
datasets>=2.20
safetensors>=0.4

# Fine-tuning / LoRA / QLoRA
peft>=0.12
trl>=0.9
bitsandbytes>=0.43

# Token counting and evaluation
tiktoken>=0.7
evaluate>=0.4
bert-score>=0.3
rouge-score>=0.1
sacrebleu>=2.4

# RAG / vector search / parsing helpers
faiss-cpu>=1.8
annoy>=1.17
pdfplumber>=0.11
beautifulsoup4>=4.12
lxml>=5.2

# Serving / streaming examples
fastapi>=0.111
uvicorn[standard]>=0.30
httpx>=0.27
requests>=2.32

# Agentic AI / LangChain examples
langchain>=0.2
langchain-community>=0.2
langchain-openai>=0.1
openai>=1.40
google-search-results>=2.4
numexpr>=2.10

# Optional RAG evaluation frameworks
ragas>=0.1
trulens>=1.0
